{
  "name": "boodschappenbaas",
  "version": "0.1.0",
  "private": true,
  "description": "Nederlandse PWA voor boodschappenlijsten per supermarkt en categorie.",
  "scripts": {
    "lint": "node --check frontend/app.js && node --check scripts/build.js && node --check scripts/update-aanbiedingen.js && for bestand in scripts/aanbiedingen-adapters/*.js; do node --check \"$bestand\"; done && node --check api/aanbiedingen.js && node --check test/mvp.test.js",
    "test": "node --test",
    "build": "node scripts/build.js"
  },
  "license": "MIT"
}
